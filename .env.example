# Copy to .env and fill in values locally (do not commit the real .env).

# Brevo API mode
BREVO_API_KEY=

# Brevo SMTP fallback mode
BREVO_SMTP_HOST=
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=
BREVO_SMTP_PASS=

# Optional sender overrides
BREVO_FROM_EMAIL=
BREVO_FROM_NAME=

